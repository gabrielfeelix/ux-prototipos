# Dependencies
node_modules

# Build output
dist

# Environment / secrets
.env*.local
.vercel
kaspersky-ca-bundle.crt

# Local tooling
.antigravitycli
*.log

# WSL artifacts
*:Zone.Identifier

# Vídeos e áudios brutos das gravações (centenas de MB cada). Ficam fora de
# public/ pra não entrar no build do Vite, e fora do git pra não inchar o
# histórico. Os clipes já cortados que o site serve vivem em public/musicos/.
assets-fonte/

# Cache do Python dos scripts em scripts/
__pycache__/
